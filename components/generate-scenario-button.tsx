import { Button } from "@/components/ui/button";
import { Text } from "lucide-react";

export default function GenerateScenarioButton() {
  return (
    <Button>
      <Text size={18} />
      Generuj scenariusz
    </Button>
  );
}
